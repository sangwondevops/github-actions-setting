name: pull-request-workflow
on:
  pull_request:
    types: [opened]

jobs:
  push-request-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1 # 생략 가능
        run: echo hello world
      - name: step2
        run: |
          echo hello world
          echo github action
