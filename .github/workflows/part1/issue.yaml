name: issue-workflow
on:
  issues: 
    types: [opened]
  
jobs: 
    issue-job: # 디폴트 브랜치에서만 트리거됨
        runs-on: ubuntu-latest 
        steps: 
        - name: step1 
          run: echo hello world
        - name: step2
          run: | 
            echo hello world
            echo github action
