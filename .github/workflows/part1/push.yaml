name: push-workflow
on: push

jobs:
  push-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "hello world"
      - name: step2
        run: | #멀티 라인 바
          echo hello world
          echo github action
