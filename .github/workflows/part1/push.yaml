name: push-workflow 12
on: push

jobs:
  push-job:
    runs-on: ubuntu-latest
    steps:
    - name: step1
      run: |
        echo hello world
        echo hello jaewook
    - name: step2
      run: |
        echo this is second step
        echo hello this is second step
