name: push-workflow
on: push

jobs:
  push-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo "Hello, World!"
      - name: step2
        run: |
          echo "Hello, World1!"
          echo "Hello, World2!"