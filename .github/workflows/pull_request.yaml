name: pull-request-workflow
on: 
  pull_request:
    types: [opened]

jobs:
  pull-requset-job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: echo hello world
      - name: step2
        run: |
          echo hello world
          echo github action  
